<local:BasePage x:TypeArguments="core:MainPageViewModel" x:Class="Library.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Library"
      xmlns:core="clr-namespace:Library.Core;assembly=Library.Core"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage">
    
    <!-- 
    
    The first page in a non-logged in mode  
    
    -->

    <!-- TODO: FLYTTA DESSA TILL BASE PAGE -->
    <Page.RenderTransform>
        <SkewTransform AngleX="0" AngleY="0" />
    </Page.RenderTransform>

    <Grid>

        <Grid.RowDefinitions>
            <!-- Login icon -->
            <RowDefinition Height="Auto" />
            <!-- Mian content -->
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Login icon -->
        <Button Content="&#xf007;" 
                FontFamily="{StaticResource FontAwesomeSolid}"
                Style="{StaticResource IconButton}"
                HorizontalAlignment="Right"
                Margin="20"
                />
        
        <!-- Main content -->
        <StackPanel Grid.Row="1" HorizontalAlignment="Center">

            <!-- Logo -->
            <Image Source="/Styling/Images/LibsysLogo.png" Width="250" Height="250" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Search bar -->
                <TextBox Style="{StaticResource SearchBox}" Tag="Sök..."
                     Width="400" Height="50"
                     BorderBrush="{StaticResource CustomLightGreyBrush}"
                     BorderThickness="2"
                     VerticalContentAlignment="Center" 
                         Text="{Binding FirstSearchText}"
                         x:Name="tbFirstSearch"
                         >
                    <TextBox.InputBindings>
                        <KeyBinding Key="Return" Command="{Binding FirstSearch}"/>
                    </TextBox.InputBindings>
                </TextBox>

                <!-- Button -->
                <Border Grid.Column="1">
                    <Button Content="&#xf061;" 
                            Style="{StaticResource SearchButton}" 
                            Margin="-12 20"
                            Command="{Binding FirstSearch}"
                            />
                </Border>

            </Grid>

            <!-- Show all books -->
            <local:ButtonWithIcon Content="Eller visa alla böcker..."
                                  Style="{StaticResource TextIconButton}"
                                  Margin="-140 20"
                                  Command="{Binding ShowAllBooks}"
                                  />

        </StackPanel>


    </Grid>
</local:BasePage>
